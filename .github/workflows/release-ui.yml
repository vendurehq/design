name: Release ui

on:
  push:
    tags:
      - "ui/v*"

concurrency:
  group: release-ui
  cancel-in-progress: false

jobs:
  release:
    name: Publish @vendure-io/ui
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Generate bot token
        id: bot-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.CI_BOT_APP_ID }}
          private-key: ${{ secrets.CI_BOT_APP_PRIVATE_KEY }}

      - name: Get bot user ID
        id: get-user
        run: echo "user-id=$(gh api "/users/${{ steps.bot-token.outputs.app-slug }}[bot]" --jq .id)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ steps.bot-token.outputs.token }}

      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ steps.bot-token.outputs.token }}

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#ui/v}" >> "$GITHUB_OUTPUT"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.9"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Update package version
        run: |
          jq --arg v "${{ steps.version.outputs.VERSION }}" '.version = $v' \
            packages/ui/package.json > tmp.json && mv tmp.json packages/ui/package.json

      - name: Build
        run: bun run build

      - name: Resolve workspace dependency
        run: |
          TOKENS_VERSION=$(jq -r '.version' packages/design-tokens/package.json)
          jq --arg v "^$TOKENS_VERSION" '.dependencies["@vendure-io/design-tokens"] = $v' \
            packages/ui/package.json > tmp.json && mv tmp.json packages/ui/package.json

      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/ui
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Commit version bump
        run: |
          git config user.name "${{ steps.bot-token.outputs.app-slug }}[bot]"
          git config user.email "${{ steps.get-user.outputs.user-id }}+${{ steps.bot-token.outputs.app-slug }}[bot]@users.noreply.github.com"
          git checkout -- packages/ui/package.json
          jq --arg v "${{ steps.version.outputs.VERSION }}" '.version = $v' \
            packages/ui/package.json > tmp.json && mv tmp.json packages/ui/package.json
          git add packages/ui/package.json
          git commit -m "chore(ui): bump version to ${{ steps.version.outputs.VERSION }}"
          git pull --rebase origin main
          git push origin HEAD:main
